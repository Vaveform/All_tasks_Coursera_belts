#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <deque>

using namespace std;

int main() {
	int initial;
	cin >> initial;
	deque<string> expression;
	deque.push_back(("(" + to_string(initial) + "+"));
//	int num_of_operation;
//	cin >> num_of_operation;
//	while(num_of_operation != 0)
//	{
//
//	}
	return 0;
}
