#include <iostream>
#include <vector>

using namespace std;

int main()
{
	int n;
	cin >> n;
	vector<int> tempretures(n);
	int sum = 0;
	for(int t : tempretures)
	{
		cin >> t;
		sum+= t;
	}
	cout << sum << endl;
	return 0;
}
