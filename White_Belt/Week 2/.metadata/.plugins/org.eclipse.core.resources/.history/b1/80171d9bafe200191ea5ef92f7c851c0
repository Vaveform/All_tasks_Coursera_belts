#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
	int n;
	cin >> n;
	vector<int> tempretures(n);
	int average = 0;
	for(int& t : tempretures)
	{
		cin >> t;
		average+= t;
	}
	average/= n;
	cout << average << endl;
	return 0;
}
