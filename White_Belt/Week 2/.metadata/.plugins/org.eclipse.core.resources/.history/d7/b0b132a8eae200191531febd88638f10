
#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <string>
#include <map>

int calendary[12] = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};
int current_position = 0;
int previous_postion = 11;

using namespace std;

void add(vector<map<int, vector<string>>>& months, int date, string task)
{
	months[months_position][date].push_back(task);
}

void next(vector<map<int, vector<string>>>& months)
{
	++months_position;
	if(months_position == 11)
	{
		months_position = 0;
	}
	if(calendary[months_position - 1] > [])
}
int main()
{
	vector<map<int, vector<string>>> months(12);

	return 0;
}
