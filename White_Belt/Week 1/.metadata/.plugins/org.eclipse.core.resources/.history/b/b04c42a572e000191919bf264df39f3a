#include <cmath>
#include <iostream>
#include <string>


using namespace std;

int main() {
	int a, b, c, x1, x2;
	cin >> a >> b >> c;
	x1 = (-b+sqrt(b*b - 4*a*c))/(2*a);
	x2 = (-b-sqrt(b*b - 4*a*c))/(2*a);
	if(x1 == x2)
	{
		cout << x1 << endl;
	}
	else if(isnan(x1) && isnan(x2))
	{
		cout << x1 << " " << x2 << endl;
	}
	else{
		cout << x1 << " " << x2 << endl;
	}
	return 0;
}
