#include <cmath>
#include <iostream>
#include <string>



using namespace std;

int main() {
	int a, b, c, x1, x2;
	cin >> a >> b >> c;
	x1 = sqrt(b*b - 4*a*c);
	x2 = sqrt(b*b - 4*a*c);
	cout << sqrt(b*b - 4*a*c) << x1 << " " << sqrt(b*b - 4*a*c) << x2 << endl;
	cout << sqrt(-4) << endl;
	return 0;
}
